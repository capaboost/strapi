/**
 * personality-recommendation controller
 */

import { factories } from '@strapi/strapi'

export default factories.createCoreController('api::personality-recommendation.personality-recommendation');
